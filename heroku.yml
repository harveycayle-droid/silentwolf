build:
  languages:
    - node        ← uses Node.js buildpack (compiles better-sqlite3 natively)
  config:
    NODE_ENV: production

run:
  web: node --no-warnings --experimental-global-webcrypto index.js
